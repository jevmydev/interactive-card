import PaymentForm from "./PaymentForm";

import "./payment.scss";

export const Payment = () => {
    return (
        <div className="Layout-payment Payment">
            <PaymentForm />
        </div>
    );
};

export default Payment;
